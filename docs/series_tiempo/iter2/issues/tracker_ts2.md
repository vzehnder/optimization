# BESS TS-2 Issue Tracker

This document is the local tracker for TS-2: generic time-series catalog in
BBDD, derived from `docs/series_tiempo/iter2/prd.md`.

External issue tracker integration has not been configured, so each issue is
stored as a Markdown file in this folder. All issues carry the
`ready-for-agent` triage label.

## Date Policy

All issues generated from this point forward include:

- `Fecha de inicio planificada`
- `Fecha de termino planificada`

Actual start/end dates can be added or corrected by the implementer when work
really begins and ends.

## Status Vocabulary

- `Todo`: not started.
- `In Progress`: actively being implemented.
- `Blocked`: waiting on a dependency or decision.
- `In Review`: implementation is ready for review.
- `Done`: merged or accepted.

## Issue Register

| ID | Title | Type | Triage | Status | Fecha de inicio planificada | Fecha de termino planificada | Blocked by | Issue file |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| BESS-TS2-000 | Review TS-2 PRD And Series Catalog Semantics | HITL | ready-for-agent | Done | 2026-07-06 | 2026-07-06 | None | [BESS-TS2-000-review-ts2-prd-and-series-catalog-semantics.md](BESS-TS2-000-review-ts2-prd-and-series-catalog-semantics.md) |
| BESS-TS2-001 | Import A Minimal CSV Time-Series Set End-To-End | AFK | ready-for-agent | Done | 2026-07-04 | 2026-07-04 | BESS-TS2-000 | [BESS-TS2-001-import-a-minimal-csv-time-series-set-end-to-end.md](BESS-TS2-001-import-a-minimal-csv-time-series-set-end-to-end.md) |
| BESS-TS2-002 | Preview Sources And Map Columns To Canonical Signals | AFK | ready-for-agent | Done | 2026-07-04 | 2026-07-04 | BESS-TS2-001 | [BESS-TS2-002-preview-sources-and-map-columns-to-canonical-signals.md](BESS-TS2-002-preview-sources-and-map-columns-to-canonical-signals.md) |
| BESS-TS2-003 | Enforce Temporal And Physical Value Validation | AFK | ready-for-agent | Done | 2026-07-13 | 2026-07-14 | BESS-TS2-002 | [BESS-TS2-003-enforce-temporal-and-physical-value-validation.md](BESS-TS2-003-enforce-temporal-and-physical-value-validation.md) |
| BESS-TS2-004 | Add XLSX Import With Sheet Selection | AFK | ready-for-agent | Done | 2026-07-15 | 2026-07-16 | BESS-TS2-003 | [BESS-TS2-004-add-xlsx-import-with-sheet-selection.md](BESS-TS2-004-add-xlsx-import-with-sheet-selection.md) |
| BESS-TS2-005 | Browse The Project Time-Series Catalog | AFK | ready-for-agent | Done | 2026-07-17 | 2026-07-20 | BESS-TS2-001 | [BESS-TS2-005-browse-the-project-time-series-catalog.md](BESS-TS2-005-browse-the-project-time-series-catalog.md) |
| BESS-TS2-006 | Edit Set Values Manually With Auditable Revisions | AFK | ready-for-agent | Done | 2026-07-21 | 2026-07-22 | BESS-TS2-003, BESS-TS2-005 | [BESS-TS2-006-edit-set-values-manually-with-auditable-revisions.md](BESS-TS2-006-edit-set-values-manually-with-auditable-revisions.md) |
| BESS-TS2-007 | Replace A Set Version With A New File Upload | AFK | ready-for-agent | Done | 2026-07-23 | 2026-07-24 | BESS-TS2-004 | [BESS-TS2-007-replace-a-set-version-with-a-new-file-upload.md](BESS-TS2-007-replace-a-set-version-with-a-new-file-upload.md) |
| BESS-TS2-008 | Harden Bulk Import Performance And Audit Metadata | AFK | ready-for-agent | Done | 2026-07-27 | 2026-07-28 | BESS-TS2-006, BESS-TS2-007 | [BESS-TS2-008-harden-bulk-import-performance-and-audit-metadata.md](BESS-TS2-008-harden-bulk-import-performance-and-audit-metadata.md) |
| BESS-TS2-009 | Finalize TS-2 Acceptance Suite And Docs | AFK | ready-for-agent | Todo | 2026-07-29 | 2026-07-29 | BESS-TS2-001 through BESS-TS2-008 | [BESS-TS2-009-finalize-ts2-acceptance-suite-and-docs.md](BESS-TS2-009-finalize-ts2-acceptance-suite-and-docs.md) |

## Recommended Execution Order

1. BESS-TS2-000 closes the PRD review and the initial canonical signal catalog.
2. BESS-TS2-001 is the tracer bullet: CSV source to persisted set, revision 1 and content hash in BBDD.
3. BESS-TS2-002 adds preview and canonical signal mapping on top of the tracer path.
4. BESS-TS2-003 hardens temporal and physical validation with row/column-tied errors.
5. BESS-TS2-004 extends the same pipeline to XLSX with sheet selection.
6. BESS-TS2-005 can proceed any time after BESS-TS2-001; it makes the catalog browsable.
7. BESS-TS2-006 adds bounded manual edits creating auditable revisions.
8. BESS-TS2-007 adds file-replacement revisions once both formats import cleanly.
9. BESS-TS2-008 closes bulk performance and audit metadata gaps across all paths.
10. BESS-TS2-009 closes the iteration with acceptance coverage and docs.

## Progress Log

| Date | Issue | Status change | Notes |
| --- | --- | --- | --- |
| 2026-07-04 | All | Created | Initial local issue set generated from the TS-2 PRD and the series hierarchy roadmap. |
| 2026-07-04 | BESS-TS2-000 | Todo -> Done | Accepted TS-2 catalog semantics in `docs/series_tiempo/iter2/decision_record_ts2_catalog_semantics.md`. No PRD correction required. Also removed TS-1 verification scenarios/cases (`TS1-00x provenance/verification` scenarios under projects 1, 5-10) from the `energy_dispatch` database so per-issue manual QA data does not keep accumulating across iterations. |
| 2026-07-04 | BESS-TS2-001 | Todo -> In Review | Implemented TS-2 tracer bullet end-to-end: generic catalog tables, deep import module, persisted source/set/revision/hash path, API readback, and React upload/import confirmation flow. Focused Python backend tests, React tests, OpenAPI generation, API drift check, and production frontend build passed. Chrome control against the local app reached login and project setup on the PostgreSQL-backed app, but final manual import verification was interrupted by another installed browser extension UI blocking automation. |
| 2026-07-04 | BESS-TS2-002 | Todo -> In Review | Extended TS-2 from single-signal tracer bullet to preview-and-map multi-signal imports: the deep import module now accepts plural `signal_mappings`, validates missing/duplicate/unknown columns and unit mismatches with column-tied errors, persists `source_column`/`source_unit` plus revision mapping audit metadata, and writes multiple `time_series_signals`/`time_series_values` rows per set. React now shows explicit preview-and-map rows before import confirmation, supports adding multiple mapped signals, and confirms the created set with all imported canonical signals. Focused TS-2 backend tests, full Python discovery (188 ok, 1 skipped), full Vitest suite (47 ok), production frontend build, `eslint .`, OpenAPI regeneration, and API drift check all passed. Chrome smoke was attempted with both `chrome:control-chrome` and `mcp__chrome_devtools`, but the sandbox could not keep a local PostgreSQL-backed app server alive and Chrome policy rejected navigation to the retry port, so manual browser QA remains pending outside the sandbox. |
| 2026-07-04 | BESS-TS2-001 | In Review -> Done | Review passed: focused TS-2 tests, full Python discovery, Vitest, eslint, build and API drift check green. Pending Chrome QA completed via `chrome-devtools` MCP against the PostgreSQL app on `http://127.0.0.1:8000` (project `TS2 Chrome QA 002`, scenario `Multi-signal import`): dual-price CSV imported end-to-end and PostgreSQL shows set `ts2_qa_dual_price` v1 with 2 signals, 3 periods, 6 values and revision 1. |
| 2026-07-04 | BESS-TS2-002 | In Review -> Done | Same review session: Chrome smoke confirmed source preview (columns plus sample rows), auto-suggested multi-signal mapping (`buy_price`/`sell_price` to import/export price signals with units) and confirmation listing both canonical signals. |
| 2026-07-04 | BESS-TS2-003 | Todo -> In Review | Implemented temporal/physical validation via TDD in `app/time_series_catalog.py`: duplicate timestamps, unordered timestamps and overlapping periods (incoherent durations) now rejected with row-tied errors; malformed timestamps carry row context; nonnumeric, nonpositive-duration, negative-nonnegative and invalid-IANA-timezone rules locked with acceptance tests. Failed imports persist nothing (validation-first plus autocommit cleanup that deletes the set row if child inserts fail). React confirmation now shows the set timezone as `America/Santiago (IANA)`. Suites: TS-2 catalog 14 ok, Python discovery 198 ok/1 skipped, Vitest 47 ok, eslint, build and api:check green. Chrome QA: duplicate-timestamp CSV surfaced `row 3: duplicate timestamp '2026-01-01T00:00:00' (already used by row 2)` with no partial set in PostgreSQL.
| 2026-07-06 | BESS-TS2-003 | In Review -> Done | Re-reviewed before starting BESS-TS2-004: `app/time_series_catalog.py` and `tests/test_ts2_time_series_catalog.py` (14 focused tests) confirm every acceptance criterion is implemented and locked (duplicate/unordered/overlapping-period rejection, malformed-timestamp/nonnumeric/negative-value row-and-column errors, no-partial-persistence on failure, IANA timezone surfaced via API and React). No further changes needed; closing as Done. |
| 2026-07-06 | BESS-TS2-004 | Todo -> Done | Implemented via TDD. `app/time_series_ingestion.py` now returns the XLSX workbook's `available_sheets` alongside `selected_sheet`, and an unknown `sheet_name` raises a clear error listing the available sheets; merged-cell and Excel-table rejections (already implemented) got locking tests. No new endpoint was needed: XLSX reuses the existing upload endpoint and the same `get_time_series_source_rows` -> `prepare_time_series_catalog_import` pipeline as CSV, so centralized validation is proven shared by a new test where an XLSX source hits the identical duplicate-timestamp rejection as CSV. React's pre-upload free-text "XLSX sheet" field was replaced with a post-upload "Sheet" dropdown (`DraftEditor.tsx`) populated from `available_sheets`; changing it re-uploads the already-selected file with the new `sheet_name`. Suites: `test_csv_time_series_ingestion` 15 ok, `test_ts2_time_series_catalog` 16 ok, full Python discovery 203 ok/1 skipped, Vitest 48 ok, `tsc -b`, eslint, `api:generate`/`api:check`, and production build all green. Chrome QA (`chrome-devtools` MCP, PostgreSQL-backed app, project "TS2 Chrome QA 004"): uploaded a two-sheet XLSX, the Sheet dropdown listed both sheets defaulting to the first, reselecting the second sheet refreshed the preview, and the catalog import created set `ts2_qa_prices` v1 with PostgreSQL recording `selected_sheet = 'Prices'` and periods matching that sheet's data, not the default sheet. |
| 2026-07-06 | BESS-TS2-005 | Todo -> Done | Implemented via TDD. Added `AnalystStore.list_time_series_sets` (`app/persistence.py`): project-scoped, joins each set to its latest revision (`revision_number`/`content_hash`) plus signal/period counts, ordered by `name`/`version_number`; wired as `GET /api/projects/{project_id}/time-series-sets` in `app/main.py`. `get_time_series_set` now also returns a `horizon` summary (`period_count`/`start`/`end`) alongside the existing full `periods`/`signals`/`values`. React adds `TimeSeriesCatalogView` (catalog list) and `TimeSeriesSetDetailView` (signals with canonical key/unit/entity, horizon summary, source provenance including XLSX sheet) under new routes `/projects/:projectId/time-series-sets` and `/projects/:projectId/time-series-sets/:timeSeriesSetId`, linked from the project detail page. All catalog/detail reads come from BBDD; no source file is reopened. Suites: 4 new focused tests in `test_ts2_time_series_catalog.py` (list summary fields, project isolation, unknown-project 404, detail horizon summary), full Python discovery 207 ok/1 skipped, Vitest 49 ok, `tsc -b`, eslint, `api:generate`/`api:check`, and production build all green. Chrome QA (`chrome-devtools` MCP, PostgreSQL-backed app, admin session): reused seeded projects "TS2 Chrome QA 002" (`ts2_qa_dual_price` v1, 2 signals, 3 periods) and "TS2 Chrome QA 004" (`ts2_qa_prices` v1, XLSX). Each project's catalog page listed only its own set with correct data_kind/status/timezone/revision/hash/signal_count/period_count; each detail page showed correct signal canonical keys/units/`Global` entity, horizon (period count + start/end instants) and source provenance, including `selected_sheet = 'Prices'` for the XLSX set. No console errors. |
| 2026-07-06 | BESS-TS2-006 | Todo -> Done | Implemented via TDD. `app/time_series_catalog.py` gained `validate_catalog_value_edits` (reuses the same numeric/nonnegative rules as import, with `edit N: <signal> at period <index>` row-tied errors for unknown signal/period, nonnumeric and negative-for-nonnegative values) and `compute_catalog_content_hash` (extracted from the import path so both flows hash the same way). `AnalystStore.edit_time_series_set_values` (`app/persistence.py`) validates edits against the set's own signals/periods before touching BBDD, then inserts a new `time_series_set_revisions` row (`time_series_source_id = NULL`, `metadata_json` recording per-edit previous/new value, `created_by`/timestamp), updates only the edited `time_series_values` rows, and refreshes `time_series_sets.content_hash`/`updated_at`; a write-phase failure deletes the just-inserted revision row (same compensating-cleanup pattern as import, since PostgreSQL runs autocommit). Added `AnalystStore.list_time_series_set_revisions` for audit lookback. New endpoints in `app/main.py`: `PUT /api/projects/{project_id}/time-series-sets/{time_series_set_id}/values` and `GET .../revisions`. React `TimeSeriesSetDetailView` (`frontend/src/Workspace.tsx`) gained a bounded editable values table (period x signal grid, one text input per cell, optional change-summary field, "Guardar correcciones"/"Descartar cambios") and a "Historial de revisiones" section listing every revision with hash/author/timestamp/change summary. Suites: 8 new focused tests in `test_ts2_time_series_catalog.py` (validation unit tests plus API/persistence revision-creation, hash-recalculation, audit-history, invalid-edit-rejects-without-mutation, unknown signal/period, empty-edit-list, 404 cases), full Python discovery 221 ok/1 skipped, Vitest 50 ok (incl. a new edit-flow test asserting the PUT payload and the resulting revision-2 UI state), `tsc -b`, `eslint .`, `api:generate`/`api:check`, and production build all green. Chrome QA (`chrome-devtools` MCP, PostgreSQL-backed app, admin session `vicente.zehnder@zenergies.cl`): created project "TS2 Chrome QA 006", imported a 2-period/2-signal CSV (`price_usd_per_mwh`, `load_demand_mw`) to set `ts2_006_price_demand` v1 revision 1, edited one price cell from 55 to 57.5 with a change summary, and confirmed the detail view moved to revision 2 with a new content hash while the revision-history list kept both revisions (correct author/timestamp/hash for each). A follow-up edit with a nonnumeric value surfaced the `edit 1: load_demand_mw at period 0 must be numeric` banner and left the set at revision 2 with the original values unchanged, confirmed directly against PostgreSQL. No console errors. |
| 2026-07-06 | BESS-TS2-007 | Todo -> Done | Implemented via TDD. `app/persistence.py` gained `AnalystStore.replace_time_series_set_source` (project/set-scoped): resolves or creates a `time_series_sources` row for the new upload, snapshots the set's current signals/periods/values, deletes and reinserts them from the freshly prepared import (reusing a new shared `_insert_time_series_signals_periods_values` helper extracted from the import path), appends a `time_series_set_revisions` row (incrementing `revision_number`, linking the new source, recalculated `content_hash`, `change_summary`) and refreshes `time_series_sets.content_hash`/`updated_at`; a write-phase failure restores the captured snapshot (new `_restore_time_series_catalog_snapshot`) before re-raising, so the current revision is provably untouched on failure (same compensating-cleanup philosophy as import/edit, since PostgreSQL runs autocommit). `name`/`version_label`/`version_number` are always taken from the existing set, never from the request, so identity cannot drift during replacement. New endpoints in `app/main.py`: `POST /api/projects/{project_id}/time-series-sets/{time_series_set_id}/replace/upload` (reuses `ingest_time_series_source` with an empty draft document, so CSV/XLSX/sheet-selection parsing is shared with the scenario-draft upload path) and `POST /api/projects/{project_id}/time-series-sets/{time_series_set_id}/replace` (reuses `read_time_series_source_rows` and `prepare_time_series_catalog_import`, so mapping/validation is identical to import). React `TimeSeriesSetDetailView` (`frontend/src/Workspace.tsx`) gained a `TimeSeriesSetReplacePanel`: file upload (with XLSX sheet dropdown once available), a data-kind/timezone/timestamp/duration-column/signal-mapping form seeded from column-name and signal suggestions, a change-summary field and a "Reemplazar set" action; on success it updates the same React Query cache entries as the manual-edit flow so the Revision/Values/history sections refresh immediately. To satisfy the `react-refresh/only-export-components` ESLint rule, the catalog-mapping helpers/constants shared between `DraftEditor.tsx` and `Workspace.tsx` were extracted into a new plain module `frontend/src/timeSeriesCatalogMapping.ts` (no behavior change, both files now import from it). Suites: 5 new focused tests in `test_ts2_time_series_catalog.py` (revision creation/identity-stability, failed-write snapshot restore, API end-to-end with identity/hash assertions, duplicate-timestamp rejection leaving the set untouched, XLSX-with-sheet-selection replacement), full Python discovery 227 ok/1 skipped, Vitest 51 ok (incl. a new replace-flow test asserting the upload/replace payloads and resulting revision-2 UI state), `tsc -b`, `eslint .`, `api:generate`/`api:check`, and production build all green. Chrome QA (`chrome-devtools` MCP, PostgreSQL-backed app, admin session `vicente.zehnder@zenergies.cl`): created project "TS2 Chrome QA 007", imported a 2-period CSV to set `ts2_007_spot_price` v1 revision 1 (hash `sha256:9638bb8f...`), then replaced it with a corrected CSV through the new detail-view panel — the set moved to revision 2 with a new hash, updated values (57.5/61.0) and updated source filename, while the revision history kept both entries with correct author/timestamp/change-summary, confirmed directly against PostgreSQL (`time_series_set_revisions` row for revision 1 still points at the original source with its original hash; a second `time_series_sources` row holds the corrected file). A follow-up replace attempt with a duplicate-timestamp CSV surfaced the shared `row 3: duplicate timestamp ...` banner and left the set at revision 2 in PostgreSQL, confirmed directly. No console errors (aside from a pre-existing, unrelated missing-`name`-attribute accessibility issue also present on the existing manual-edit form). |
| 2026-07-06 | BESS-TS2-008 | Todo -> Done | Implemented via TDD. `app/database.py` now exposes `PostgresConnection.executemany`, and `app/persistence.py` routes `time_series_values` writes through a shared bulk insert helper so realistic imports/replacements stop doing one insert per value while preserving the existing snapshot-restore cleanup path. Error surfacing in `app/main.py` now prefixes validation failures with source filename and XLSX sheet context, producing consistent `source '<file>'[, sheet '<sheet>']: row N: ...` messages across draft import and replacement flows. Revision lineage in `app/persistence.py` is completed with `superseded_revision_number`, preserved `time_series_source_id` on manual edits, and source lookup for both current-set detail and revision history so every revision now carries author/date/source/superseded metadata even after edit -> replace chains. Suites: 4 new focused TS-2 tests (bulk insert on realistic-size import, CSV source-context error, XLSX source+sheet-context error, edit/replace audit-lineage survival), full `tests.test_ts2_time_series_catalog` (44 ok), full Python discovery (231 ok, 1 skipped), frontend Vitest (51 ok), production build and `api:check` all green. Chrome QA used both `chrome:control-chrome` and `chrome-devtools`: on PostgreSQL-backed app `http://127.0.0.1:8000`, project `TS2 Chrome QA 008` / set `price` v1 imported from `price.csv`, manual edit created revision 2, invalid XLSX replacement surfaced `source 'price_invalid.xlsx', sheet 'Prices': row 3: spot_price must be numeric`, and valid CSV replacement moved the set to revision 3 with updated values (57.5/61.0) and source `price_corrected.csv`; PostgreSQL then confirmed revision lineage `3 -> 2 -> 1` with the expected source metadata on every revision. |


## Regression Guard

Every slice that changes backend persistence must keep the existing Python
suite green: scenario versions, structured drafts, hydraulic diagrams, manual
runs and TS-1 hierarchy provenance tests.

Slices changing React should run the relevant frontend unit tests, `tsc -b`
and `eslint .`.

No slice in TS-2 should touch Julia-facing payloads; if one accidentally does,
the Julia optimizer regression suite must be run.

TS-2 must not migrate or break the existing hydraulic-specific time-series
tables or the draft-embedded CSV/XLSX ingestion; those remain legacy paths
until TS-5.
